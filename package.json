{
  "name": "odiadev-mcp-server",
  "version": "3.2.1",
  "private": true,
  "description": "ODIADEV MCP Server - serverless APIs (TTS + payments + webhooks)",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test"
  },
  "dependencies": {}
}