{
  "name": "odiadev-mcp-server",
  "version": "3.2.0",
  "private": true,
  "description": "ODIADEV MCP Server - serverless APIs (TTS + payments + webhooks)",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {}
}